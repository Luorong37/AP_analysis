# Legacy baseline

`snapshot_2026-07-30` is a mechanical copy of the current working-copy
sources at the start of AAA construction. It is not taken from Git HEAD.
Files in this directory must never be placed on the MATLAB path.

`SOURCE_HASHES.txt` records SHA-256 hashes for regression provenance.
