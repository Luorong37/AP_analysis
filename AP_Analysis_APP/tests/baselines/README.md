# Regression baselines

Large movies are not copied into the AAA source tree. Baseline MAT files,
field inventories, and comparison reports generated from user-selected
fixtures belong here.

Scientific values are compared exactly first. If GPU or parallel execution
produces floating-point differences, the observed differences must be
reported before a tolerance is approved; no tolerance is assumed silently.
